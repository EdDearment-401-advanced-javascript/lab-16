'use strict';


describe('truth', () => {
  it('should return an error when file not found', () => {
    let theTruth = true;
    expect(theTruth).toBe(true);
  });

  it('should read a file.', () => {
    let theTruth = true;
    expect(theTruth).toBe(true);
  });

  it('should ')
});